.row
  %table.table.table-bordered
    %thead
      %tr
        %th
          Title
        %th
          Description
    %tbody
    - @issues.sort_by(&:created_at).reverse.each do |issue|
      %tr
        %td= issue.title
        %td
          %a{:href => issue.image_url}
            =issue.description
